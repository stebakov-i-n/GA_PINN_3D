# OpenFOAM Templates

The first phase writes compact OpenFOAM dictionaries directly from
`cfd.openfoam.generate_openfoam_case`. This directory is reserved for larger
case templates if later vascular meshing requires them.
